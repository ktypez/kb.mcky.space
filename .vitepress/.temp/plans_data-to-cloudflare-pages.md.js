import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Plan: Migrate Data Project to Cloudflare Pages","description":"","frontmatter":{"id":"plan-data-to-cloudflare-pages","version":2,"status":"completed","created":"2026-07-10T00:00:00.000Z","updated":"2026-07-10T00:00:00.000Z"},"headers":[],"relativePath":"plans/data-to-cloudflare-pages.md","filePath":"plans/data-to-cloudflare-pages.md","lastUpdated":null}');
const _sfc_main = { name: "plans/data-to-cloudflare-pages.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="plan-migrate-data-project-to-cloudflare-pages" tabindex="-1">Plan: Migrate Data Project to Cloudflare Pages <a class="header-anchor" href="#plan-migrate-data-project-to-cloudflare-pages" aria-label="Permalink to &quot;Plan: Migrate Data Project to Cloudflare Pages&quot;">​</a></h1><h2 id="goal" tabindex="-1">Goal <a class="header-anchor" href="#goal" aria-label="Permalink to &quot;Goal&quot;">​</a></h2><p>ย้าย data project (CRM client management) จาก Vercel (Next.js 16) ไป Cloudflare Pages — ใช้ Vite SPA + Pages Functions, DB คงเป็น Neon HTTP (ไม่เปลี่ยน), ไม่มี PWA</p><h2 id="architecture" tabindex="-1">Architecture <a class="header-anchor" href="#architecture" aria-label="Permalink to &quot;Architecture&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>data.mcky.space</span></span>
<span class="line"><span>├── /app/*            — Vite SPA (React 19 + React Router)</span></span>
<span class="line"><span>├── /api/*            — Cloudflare Pages Functions (Neon, Auth, R2)</span></span>
<span class="line"><span>└── /assets/*         — Static assets</span></span></code></pre></div><ul><li>DB: Neon (คงเดิม) ผ่าน <code>@neondatabase/serverless</code> HTTP + <code>drizzle-orm/neon-http</code></li><li>API: Cloudflare Pages Functions (Workers)</li><li>Auth: <code>crypto.scryptSync</code> → Web Crypto API (<code>crypto.subtle.pbkdf2</code>)</li><li>Storage: AWS SDK S3 → Workers R2 binding</li><li>No PWA component</li></ul><h2 id="schema-—-ไม่เปลี่ยนแปลง" tabindex="-1">Schema — ไม่เปลี่ยนแปลง <a class="header-anchor" href="#schema-—-ไม่เปลี่ยนแปลง" aria-label="Permalink to &quot;Schema — ไม่เปลี่ยนแปลง&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Table</th><th>Action</th></tr></thead><tbody><tr><td><code>clients</code></td><td>คงเดิม — <code>pgTable</code>, <code>jsonb</code>, <code>bigint</code>, <code>real</code></td></tr><tr><td><code>suggestions</code></td><td>คงเดิม</td></tr><tr><td><code>settings</code></td><td>คงเดิม</td></tr></tbody></table><h2 id="phase-1-—-scaffold-vite-react-ts-project" tabindex="-1">Phase 1 — Scaffold Vite + React + TS Project <a class="header-anchor" href="#phase-1-—-scaffold-vite-react-ts-project" aria-label="Permalink to &quot;Phase 1 — Scaffold Vite + React + TS Project&quot;">​</a></h2><ul><li>[ ] สร้าง Vite project ด้วย <code>npm create vite@latest</code></li><li>[ ] ติดตั้ง dependencies: React Router, Tailwind CSS, shadcn/ui</li><li>[ ] ตั้งค่า <code>wrangler.toml</code> สำหรับ Cloudflare Pages</li><li>[ ] คัดลอก <code>tsconfig.json</code> + ปรับ paths</li><li>[ ] ตั้งค่า <code>vite.config.ts</code></li></ul><h2 id="phase-2-—-port-client-side-layer-copy-path-tweaks" tabindex="-1">Phase 2 — Port Client-Side Layer (copy + path tweaks) <a class="header-anchor" href="#phase-2-—-port-client-side-layer-copy-path-tweaks" aria-label="Permalink to &quot;Phase 2 — Port Client-Side Layer (copy + path tweaks)&quot;">​</a></h2><ul><li>[ ] Components (ทั้งหมด 41 ไฟล์, ไม่รวม ui/) <ul><li><code>next/image</code> → <code>&amp;lt;img&amp;gt;</code> (AppImage.tsx)</li><li><code>next/link</code> → React Router <code>&amp;lt;Link&amp;gt;</code></li><li><code>useRouter</code> → <code>useNavigate()</code></li></ul></li><li>[ ] Hooks (6 ไฟล์) — copy as-is</li><li>[ ] Stores (5 Zustand) — copy as-is</li><li>[ ] Types (2 ไฟล์) — copy as-is</li><li>[ ] Design tokens (<code>lib/design/</code>) — copy as-is</li><li>[ ] Client libs (<code>lib/utils.ts</code>, <code>lib/storage.ts</code>, <code>lib/compressImage.ts</code>, <code>lib/offline-db.ts</code>) — copy as-is</li><li>[ ] Pages: port <code>app/page-client.tsx</code> → <code>src/pages/Clients.tsx</code></li><li>[ ] Pages: port <code>app/c/[id]/client-page.tsx</code> → <code>src/pages/ClientDetail.tsx</code></li><li>[ ] <code>app/layout.tsx</code> → port to <code>src/App.tsx</code> + <code>src/main.tsx</code></li><li>[ ] <code>app/providers.tsx</code> — port, remove <code>next-themes</code></li><li>[ ] <code>app/globals.css</code> — copy as-is</li></ul><h2 id="phase-3-—-rewrite-server-layer" tabindex="-1">Phase 3 — Rewrite Server Layer <a class="header-anchor" href="#phase-3-—-rewrite-server-layer" aria-label="Permalink to &quot;Phase 3 — Rewrite Server Layer&quot;">​</a></h2><ul><li>[ ] <code>lib/auth.ts</code> — rewrite <code>crypto.scryptSync</code>/<code>createHmac</code>/<code>fs</code> → Web Crypto API (PBKDF2 + HMAC) + KV for local fallback</li><li>[ ] <code>lib/r2.ts</code> — rewrite AWS SDK v3 <code>S3Client</code> → Workers R2 binding (<code>env.BUCKET.put()</code>/<code>delete()</code>)</li><li>[ ] <code>lib/db/index.ts</code> — ปรับเล็กน้อย (Neon HTTP ใช้ได้แล้วบน Workers)</li></ul><h2 id="phase-4-—-port-api-routes" tabindex="-1">Phase 4 — Port API Routes <a class="header-anchor" href="#phase-4-—-port-api-routes" aria-label="Permalink to &quot;Phase 4 — Port API Routes&quot;">​</a></h2><ul><li>[ ] <code>app/api/auth/route.ts</code> → <code>functions/api/auth.ts</code></li><li>[ ] <code>app/api/clients/route.ts</code> → <code>functions/api/clients.ts</code></li><li>[ ] <code>app/api/clients/[id]/route.ts</code> → <code>functions/api/clients/[id].ts</code></li><li>[ ] <code>app/api/clients/search/route.ts</code> → <code>functions/api/clients/search.ts</code></li><li>[ ] <code>app/api/clients/count/route.ts</code> → <code>functions/api/clients/count.ts</code></li><li>[ ] <code>app/api/clients/trash/route.ts</code> → <code>functions/api/clients/trash.ts</code></li><li>[ ] <code>app/api/cleanup-trash/route.ts</code> → <code>functions/api/cleanup-trash.ts</code> (cron trigger)</li><li>[ ] <code>app/api/photo-request/route.ts</code> → <code>functions/api/photo-request.ts</code></li><li>[ ] <code>app/api/suggestions/route.ts</code> → <code>functions/api/suggestions.ts</code></li><li>[ ] <code>app/api/suggestions/[id]/route.ts</code> → <code>functions/api/suggestions/[id].ts</code></li></ul><h2 id="phase-5-—-deploy-config" tabindex="-1">Phase 5 — Deploy Config <a class="header-anchor" href="#phase-5-—-deploy-config" aria-label="Permalink to &quot;Phase 5 — Deploy Config&quot;">​</a></h2><ul><li>[ ] <code>wrangler.toml</code> — build commands, env vars, cron trigger</li><li>[ ] env secrets (DATABASE_URL, R2 binding, TOKEN_SECRET, ADMIN_PASSWORD)</li><li>[ ] Domain: <code>data.mcky.space</code> → Cloudflare</li><li>[ ] Smoke test: CRUD, auth, search, suggestions, R2 uploads</li></ul><h2 id="files-impacted-summary" tabindex="-1">Files Impacted (summary) <a class="header-anchor" href="#files-impacted-summary" aria-label="Permalink to &quot;Files Impacted (summary)&quot;">​</a></h2><table tabindex="0"><thead><tr><th>File</th><th>Action</th></tr></thead><tbody><tr><td><code>app/api/*</code> (10 routes)</td><td>Rewrite → <code>functions/api/*</code></td></tr><tr><td><code>lib/auth.ts</code></td><td>Rewrite — Web Crypto + KV</td></tr><tr><td><code>lib/r2.ts</code></td><td>Rewrite — R2 binding</td></tr><tr><td><code>lib/clients.ts</code></td><td>Copy, drop <code>isAuthorized</code> (moved to functions)</td></tr><tr><td><code>components/*</code> (41)</td><td>Copy + replace Next.js APIs</td></tr><tr><td><code>hooks/*</code>, <code>stores/*</code>, <code>types/*</code></td><td>Copy as-is</td></tr><tr><td><code>lib/db/index.ts</code></td><td>Minor tweak</td></tr><tr><td><code>app/page.tsx</code> + page-client</td><td>Rewrite → <code>src/pages/Clients.tsx</code></td></tr><tr><td><code>app/c/[id]/page.tsx</code> + client</td><td>Rewrite → <code>src/pages/ClientDetail.tsx</code></td></tr><tr><td><code>app/layout.tsx</code></td><td>Rewrite → <code>src/App.tsx</code></td></tr><tr><td><code>app/providers.tsx</code></td><td>Adapt (remove next-themes)</td></tr><tr><td><code>next.config.ts</code></td><td>Remove</td></tr><tr><td><code>vercel.json</code></td><td>Remove</td></tr><tr><td><code>package.json</code></td><td>Replace scripts</td></tr></tbody></table><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><ul><li>[ ] <code>pnpm build</code> — Vite build + Functions build</li><li>[ ] Dev: <code>pnpm dev</code> — Vite dev server + Functions local</li><li>[ ] Auth: login/logout/change password</li><li>[ ] CRUD: create/view/edit/delete client</li><li>[ ] Search: name/shop name</li><li>[ ] Suggestions: approve/reject</li><li>[ ] R2: upload client images</li><li>[ ] Lightbox, map, offline-db</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("plans/data-to-cloudflare-pages.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const dataToCloudflarePages = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  dataToCloudflarePages as default
};
