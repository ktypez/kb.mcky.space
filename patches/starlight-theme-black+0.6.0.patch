diff --git a/node_modules/starlight-theme-black/src/components/Drawer.astro b/node_modules/starlight-theme-black/src/components/Drawer.astro
index 109de9c..7a273f7 100644
--- a/node_modules/starlight-theme-black/src/components/Drawer.astro
+++ b/node_modules/starlight-theme-black/src/components/Drawer.astro
@@ -13,9 +13,8 @@ const { siteTitleHref, sidebar } = Astro.locals.starlightRoute
 ---
 
-<div popover="auto" id="drawer">
-  <button class="sheet-overlay" popovertarget="drawer" popovertargetaction="hide"></button>
-  <div class="content">
+<div popover="auto" id="drawer" data-close-on-backdrop>
+  <div class="content">
     <div class="header">
       <button class="drag-icon" popovertarget="drawer" popovertargetaction="hide"><span></span></button>
     </div>
@@ -30,18 +29,28 @@ const { siteTitleHref, sidebar } = Astro.locals.starlightRoute
           sidebar.map(
             (entry) =>
               entry.type !== 'link' && (
-                <div class="container-sidebar-entry">
-                  <h4 class="entry-title">{entry.label}</h4>
-
+                <div class="container-sidebar-entry" data-sidebar-group>
+                  <button class="entry-title" data-toggle-group aria-expanded="true">
+                    <svg class="chevron" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"/></svg>
+                    {entry.label}
+                  </button>
+
                   {entry.entries.map(
                     (link) =>
                       link.type === 'link' && (
-                        <a href={link.href} {...link.attrs}>
+                        <a href={link.href} {...link.attrs} data-close-drawer>
                           {link.label}
                         </a>
                       ),
                   )}
                 </div>
               ),
           )
         }
       </div>
     </div>
   </div>
 </div>
 
+<script>
+  const drawer = document.getElementById('drawer')
+  if (!drawer) throw new Error('#drawer not found')
+
+  // Close drawer when clicking backdrop (the popover element itself)
+  drawer.addEventListener('click', (e) => {
+    if (e.target === drawer) {
+      drawer.hidePopover()
+    }
+  })
+
+  // Close drawer when clicking a sidebar link
+  drawer.addEventListener('click', (e) => {
+    const link = e.target.closest('[data-close-drawer]')
+    if (link) {
+      // Give the browser a tick to navigate before hiding
+      setTimeout(() => drawer.hidePopover(), 0)
+    }
+  })
+
+  // Collapsible sidebar groups
+  drawer.addEventListener('click', (e) => {
+    const toggle = e.target.closest('[data-toggle-group]')
+    if (!toggle) return
+    const group = toggle.closest('[data-sidebar-group]')
+    if (!group) return
+    const isExpanded = toggle.getAttribute('aria-expanded') === 'true'
+    toggle.setAttribute('aria-expanded', String(!isExpanded))
+    group.classList.toggle('collapsed')
+  })
+</script>
+
 <style>
   button {
     all: unset;
   }
 
   [popover] {
     position: fixed;
-    top: 0;
-    left: 0;
-    width: 100vw;
-    height: 100vh;
+    inset: 0;
     display: flex;
     opacity: 0;
     pointer-events: none;
     align-items: center;
     flex-direction: column;
     justify-content: flex-end;
-    transition: 0.1s linear;
-    z-index: 1000;
-    background: transparent;
-    border: none;
-  }
-
-  [popover]::backdrop {
-    background: rgba(0, 0, 0, 0.8);
-  }
+    transition: opacity 0.15s linear;
+    z-index: 9999;
+    background: transparent;
+    border: none;
+  }
 
-  [popover]:popover-open {
-    opacity: 1;
-    pointer-events: auto;
+  [popover]::backdrop {
+    background: rgba(0, 0, 0, 0.8);
   }
 
-  [popover]:popover-open .content {
-    transform: translateY(0%);
+  [popover]:popover-open {
+    opacity: 1;
+    pointer-events: auto;
   }
 
-  .sheet-overlay {
-    position: fixed;
-    top: 0;
-    left: 0;
-    z-index: -1;
-    width: 100vw;
-    height: 100vh;
-    opacity: 0.1;
-    background-color: transparent;
+  [popover]:popover-open .content {
+    transform: translateY(0%);
   }
 
   .content {
@@ -124,7 +175,38 @@ const { siteTitleHref, sidebar } = Astro.locals.starlightRoute
     display: flex;
     flex-direction: column;
     gap: 0.5rem;
   }
 
   .entry-title {
-    font-weight: 500;
+    all: unset;
+    cursor: pointer;
+    display: flex;
+    align-items: center;
+    gap: 0.25rem;
+    font-weight: 500;
+    font-size: 1rem;
+    line-height: 1.5rem;
+    user-select: none;
+  }
+
+  .entry-title:hover {
+    opacity: 0.8;
+  }
+
+  .chevron {
+    flex-shrink: 0;
+    transition: transform 0.15s ease;
+  }
+
+  [data-sidebar-group].collapsed .chevron {
+    transform: rotate(-90deg);
+  }
+
+  [data-sidebar-group].collapsed > a,
+  [data-sidebar-group].collapsed > [data-close-drawer] {
+    display: none;
   }
 
   .container-sidebar-entry a {
diff --git a/node_modules/starlight-theme-black/src/overrides/SidebarSublist.astro b/node_modules/starlight-theme-black/src/overrides/SidebarSublist.astro
index 214adc8..5e3673c 100644
--- a/node_modules/starlight-theme-black/src/overrides/SidebarSublist.astro
+++ b/node_modules/starlight-theme-black/src/overrides/SidebarSublist.astro
@@ -25,14 +25,31 @@ const { sublist, nested } = Astro.props
         {entry.badge && <span class="entry-badge">{entry.badge.text}</span>}
       </a>
     ) : (
-      <div class="container-sidebar-entry">
-        <h4 class="entry-title">{entry.label}</h4>
+      <div class="container-sidebar-entry" data-sidebar-group>
+        <button class="entry-title" data-toggle-group aria-expanded="true">
+          <svg class="chevron" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"/></svg>
+          {entry.label}
+        </button>
         <div class="container-group-link">
           <Astro.self sublist={entry.entries} nested />
         </div>
       </div>
     ),
   )
 }
 
+<script>
+  document.addEventListener('click', (e) => {
+    const toggle = e.target.closest('[data-toggle-group]')
+    if (!toggle) return
+    const group = toggle.closest('[data-sidebar-group]')
+    if (!group) return
+    const isExpanded = toggle.getAttribute('aria-expanded') === 'true'
+    toggle.setAttribute('aria-expanded', String(!isExpanded))
+    group.classList.toggle('collapsed')
+  })
+</script>
+
 <style>
+  button {
+    all: unset;
+  }
+
   .container-sidebar-entry {
     gap: 0.25rem;
     flex-direction: column;
     display: flex;
   }
 
   .entry-title {
-    font-weight: 600;
-    font-size: 0.875rem;
-    line-height: 1.25rem;
+    all: unset;
+    cursor: pointer;
+    display: flex;
+    align-items: center;
+    gap: 0.25rem;
+    font-weight: 600;
+    font-size: 0.875rem;
+    line-height: 1.25rem;
     padding-top: 0.25rem;
     padding-bottom: 0.25rem;
     padding-left: 0.5rem;
     padding-right: 0.5rem;
     border-radius: calc(var(--radius) - 2px);
+    user-select: none;
+  }
+
+  .entry-title:hover {
+    background-color: var(--accent);
+  }
+
+  .chevron {
+    flex-shrink: 0;
+    transition: transform 0.15s ease;
+  }
+
+  [data-sidebar-group].collapsed .chevron {
+    transform: rotate(-90deg);
+  }
+
+  [data-sidebar-group].collapsed > .container-group-link {
+    display: none;
   }
 
   .container-group-link {
diff --git a/node_modules/starlight-theme-black/src/styles/base.css b/node_modules/starlight-theme-black/src/styles/base.css
index 950a09f..1d9f56f 100644
--- a/node_modules/starlight-theme-black/src/styles/base.css
+++ b/node_modules/starlight-theme-black/src/styles/base.css
@@ -10,7 +10,8 @@
 
   *,
-  :after :before {
+  ::after,
+  ::before {
     box-sizing: border-box;
     border: 0 solid #e5e7eb;
   }
